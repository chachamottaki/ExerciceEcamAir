<!DOCTYPE html>
<html>
<head>
   <title><%= (typeof title != "undefined" ? title : "") %></title>
   <link rel="stylesheet" href="public/css/style.css">
</head>
<body>
   <h1><%= (typeof title != "undefined" ? heading : "") %></h1>
   <p><%= (typeof title != "undefined" ? content : "") %></p>
   <form method="POST" action="/submit">
      <label for="destination"><%= field1 %></label>
      <input type="text" required>
      <br>
      <label for="amount"><%= field2 %></label>
      <input type="text" required>
      <br>
      <label for="insurance"><%= field3 %></label>
      <input type="checkbox">
      <br>
      <button type="submit"><%= formbtn %></button>
    </form>

    <button type="submit"><%= bodybtn %></button>
    <br>
    <label><%= field4 %></label>
</body>
</html>
